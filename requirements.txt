# PlantSwarm runtime dependencies.
# Pipeline:
#   Phase 0          pathome_kb        — Claude-driven canonical KB build (LOCAL)
#   Phase 0R         plantswarm        — Qwen-swarm regional delta extraction
#                                        (needs an OpenAI-compatible vLLM
#                                         endpoint serving Qwen2.5-VL-7B-Instruct)
#   Phase PathomeOOD train_and_eval    — BioCLIP-init two-projector CLIP trained
#                                        on Bugwood with KB-grounded captions
#                                        (A100/H100-class GPU host)

# ---- LOCAL + light GPU host installs --------------------------------------
# Setup + image cache
numpy>=1.24.0
Pillow>=10.0.0

# Phase 0 + verifier — all Claude calls go through the `claude -p`
# headless CLI (authenticated via `claude` login). No API key path,
# no `anthropic` Python SDK dependency.
httpx>=0.27.0
openpyxl>=3.1.0
python-dotenv>=1.0.0
requests>=2.31.0
tqdm>=4.66.0

# Config loading
pyyaml>=6.0

# Captions parquet + paper-table aggregation
pandas>=2.0.0
pyarrow>=14.0.0

# Step 5 — TabPFN tabular foundation classifier
# (frozen encoder + KB-caption + crop_text + state_text -> TabPFN over a
#  15-variant feature ablation matrix on top of 7 encoders.)
tabpfn>=2.0.0
scikit-learn>=1.4.0       # PCA reducer + utilities

# Step 5 — Grad-CAM (BioCAP §C.3 reproduction) + figure triptychs.
matplotlib>=3.8.0

# Tests
pytest>=7.0.0


# ---- GPU host only (Phase 0R vLLM + step 4 encoder fine-tune +
#                    step 5 frozen-encoder forward) -----------------------
# These are installed manually on the host that does GPU work — NOT
# auto-installed by `pip install -r requirements.txt` because they pull
# in CUDA wheels (multi-GB). On Nova:
#
#   pip install vllm                  # Phase 0R (step 2) only:
#                                     #   Qwen2.5-VL-7B server
#   pip install torch                 # step 4 train, step 5 forward
#   pip install transformers          # tokenizers + HF model loaders
#   pip install open_clip_torch       # CLIP factory + tokenizer for all
#                                     #   7 encoders in step 5
#   pip install huggingface_hub       # hf-hub:imageomics/bioclip etc.
#   pip install webdataset accelerate # step 4 BioCAP-style encoder
#                                     #   fine-tune (sh_04_train_encoder)
#
# Minimum versions:
#   vllm>=0.4.0  torch>=2.1.0  transformers>=4.40.0
#   open_clip_torch>=2.29.0  huggingface_hub>=0.24.0
#   webdataset>=0.2.86  accelerate>=0.30.0
